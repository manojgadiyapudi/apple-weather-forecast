# Files in the config/locales directory are used for internationalization
# and are automatically loaded by Rails. If you want to use locales other
# than English, add the necessary files in this directory.
#
# To use the locales, use `I18n.t`:
#
#     I18n.t 'hello'
#
# In views, this is aliased to just `t`:
#
#     <%= t('hello') %>
#
# To use a different locale, set it with `I18n.locale`:
#
#     I18n.locale = :es
#
# This would use the information in config/locales/es.yml.
#
# To learn more, please read the Rails Internationalization guide
# available at http://guides.rubyonrails.org/i18n.html.

en:
  hello: "Hello world"
  parsing_error: "There was an error parsing your request."
  forecast_service:
    config:
      base_url_missing: 'Weather API base URL is not configured'
      api_key_missing: 'OpenWeatherMap API key is not configured'
    messages:
      success: 'Weather forecast retrieved successfully.'
      error: 'Failed to retrieve weather forecast.'
      no_data_found: 'No forecast data found for ZIP code %{zip_code}'
  forecast_controller:
    alerts:
      enter_zip: 'Please enter a ZIP code'
      enter_country: 'Please enter a country code'
      zip_length: 'ZIP code must be at least 4 characters long'
      country_length: 'Country code must be 2 characters long'
    messages:
      success: 'Forecast data retrieved successfully.'
      error: 'Error fetching forecast data: %{error}'
